{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME=aiagent-default}"
    },
    "location": {
      "value": "${AZURE_LOCATION=eastus}"
    },
    "principalId": {
      "value": "${AZURE_PRINCIPAL_ID=}"
    },
    "openAiModelName": {
      "value": "${openAiModelName=gpt-4o}"
    },
    "openAiModelVersion": {
      "value": "${openAiModelVersion=2025-08-07}"
    },
    "openAiModelCapacity": {
      "value": "${openAiModelCapacity=50}"
    }
  }
}
