# Azure AI Foundry Configuration (Foundry Agent Service)
# Get this from your Azure AI Foundry project or config.json after Lab 1
# Format: https://<your-project>.services.ai.azure.com/api/projects/<project-id>
# This is automatically generated in Lab 3 from config.json
PROJECT_CONNECTION_STRING=https://your-project-name.services.ai.azure.com/api/projects/your-project-id

# Azure AI Search Configuration (for Research Agent RAG)
# Get these from your Azure AI Search service
# SEARCH_ENDPOINT is automatically set in Lab 3 from config.json
# SEARCH_KEY is retrieved via Azure CLI in Lab 3
# SEARCH_INDEX is the name of your knowledge base index (created in Lab 2)
SEARCH_ENDPOINT=https://your-search-service.search.windows.net/
SEARCH_KEY=your-search-admin-key-here
SEARCH_INDEX=ai-agent-knowledge-base

# MCP Server Configuration (for Tool Agent)
# The endpoint of your Model Context Protocol server for external tools
# Tools: weather API (real-time data from wttr.in)
# This is automatically set in Lab 3 from config.json (mcp_endpoint)
MCP_ENDPOINT=https://your-mcp-server.azurecontainerapps.io

# Application Insights Configuration (for Application Analytics)
# Get this from your Application Insights resource in Azure Portal
# This is automatically retrieved in Lab 3
# Optional: Remove this section if you don't want to use Application Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key;IngestionEndpoint=https://region.in.applicationinsights.azure.com/

# OpenTelemetry Configuration (for Tracing)
# These settings control how telemetry data is collected and exported to Azure Monitor
OTEL_SERVICE_NAME=azure-ai-agent
OTEL_TRACES_EXPORTER=azure_monitor
OTEL_METRICS_EXPORTER=azure_monitor
OTEL_LOGS_EXPORTER=azure_monitor
OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true

# GenAI Content Recording (Prompt/Completion in Tracing UI)
# Enable this to see Input/Output in Azure AI Foundry Tracing UI
# WARNING: This may expose sensitive data (prompts, completions) - use with caution in production
# Options: true (enable) or false (disable)
# For production: Consider disabling or implementing masking/sampling
AZURE_TRACING_GEN_AI_CONTENT_RECORDING_ENABLED=true

# Masking / PII Handling
# Controls how sensitive information is masked in traces
# Options: off (no masking), standard (basic masking), strict (aggressive masking)
# This is used by the masking.py utility to protect sensitive data
AGENT_MASKING_MODE=standard

# (Optional) Sampling Configuration for High Traffic Environments
# Uncomment to enable trace sampling to reduce costs and storage
# OTEL_TRACES_SAMPLER=parentbased_traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.2  # 20% sampling rate

# Authentication
# For local development: Uses Azure CLI credentials (az login)
# For Container Apps deployment: Uses Managed Identity (no credentials needed)
